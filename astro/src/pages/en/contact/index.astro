---
import BaseLayout from "../../../layouts/BaseLayout.astro";

const contactLinks = [
    { slug: "anonymous-feedback", title: "Anonymous Feedback" },
    { slug: "contact-board", title: "Contact Board" },
    { slug: "report-notification", title: "Report Safety Issues" },
    { slug: "who-to-ask", title: "FAQ / Who to Ask" },
];
---

<BaseLayout title="Contact Us">
    <section class="section">
        <div class="container">
            <h1 class="page-title">Contact Us</h1>
            <p class="intro">
                We'd love to hear from you. Choose the most relevant option
                below.
            </p>

            <div class="tile-grid">
                {
                    contactLinks.map((link) => (
                        <div class="tile">
                            <h3>{link.title}</h3>
                            <p>
                                Skeleton contact form or info for {link.title}.
                            </p>
                            <a
                                href={`/en/contact/${link.slug}`}
                                class="btn btn-black"
                            >
                                Go to page
                            </a>
                        </div>
                    ))
                }
            </div>
        </div>
    </section>
</BaseLayout>

<style>
    .page-title {
        font-size: 3rem;
        margin-bottom: 1rem;
    }
    .intro {
        font-size: 1.2rem;
        color: #666;
        margin-bottom: 3rem;
    }
    .tile-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 1.5rem;
    }
    .tile {
        background: var(--color-gray-light);
        padding: 2rem;
        border-radius: 8px;
    }
    .tile h3 {
        margin-bottom: 1rem;
    }
    .btn {
        margin-top: 1rem;
        font-size: 0.8rem;
    }
</style>
