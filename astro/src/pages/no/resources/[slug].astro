---
import BaseLayout from "../../../layouts/BaseLayout.astro";

export function getStaticPaths() {
    return [
        {
            params: { slug: "showhowtodo" },
            props: { title: "Show-manual" },
        },
        {
            params: { slug: "workshophowtodo" },
            props: { title: "Workshop-manual" },
        },
    ];
}

const { slug } = Astro.params;
const { title } = Astro.props;
---

<BaseLayout title={title}>
    <section class="section">
        <div class="container">
            <h1 class="page-title">{title}</h1>
            <div class="content">
                <p>
                    Dette er en skjelettside for <strong>{title}</strong>. Denne
                    ressursen er designet for å hjelpe våre frivillige med å
                    drive vellykkede arrangementer.
                </p>

                <div class="placeholder-block">
                    [Prosedyrer og retningslinjer for {slug} vil bli plassert her]
                </div>
            </div>
        </div>
    </section>
</BaseLayout>

<style>
    .page-title {
        font-size: 3rem;
        margin-bottom: 2rem;
    }
    .content {
        max-width: 800px;
        font-size: 1.2rem;
    }
    .placeholder-block {
        margin-top: 3rem;
        padding: 4rem;
        background: var(--color-gray-light);
        border: 2px dashed #ccc;
        text-align: center;
        color: #999;
    }
</style>
